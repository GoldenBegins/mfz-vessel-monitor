(()=>{'use strict';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt=v=>Number.isFinite(Number(v))?new Intl.NumberFormat('en-US',{maximumFractionDigits:0}).format(Number(v)):'—';
function ensure(){if(document.getElementById('cargo-capacity-intelligence'))return;const main=document.querySelector('main'),nav=document.querySelector('aside nav');if(!main||!nav)return;
const s=document.createElement('section');s.id='cargo-capacity-intelligence';s.className='view';s.innerHTML=`<article class="panel"><div class="panel-head"><div><span class="kicker">Cargo Capacity Intelligence</span><h2>تقدير حمولة البضائع العامة</h2></div><span class="pill" id="cciUpd">—</span></div><p class="muted">تقديرات تخطيطية مبنية على سجل MFZ التاريخي. ليست أطنانًا رسمية مناولة.</p><div class="cards" id="cciCards"></div><div class="table-wrap"><table><thead><tr><th>الميناء</th><th>السفينة</th><th>IMO</th><th>DWT</th><th>الطريقة</th><th>الطن المقدر</th><th>الثقة</th></tr></thead><tbody id="cciRows"></tbody></table></div></article>`;main.appendChild(s);
const b=document.createElement('button');b.className='nav';b.dataset.view='cargo-capacity-intelligence';b.textContent='تقدير حمولة البضائع';nav.appendChild(b)}
async function load(){ensure();try{const d=await fetch(`cargo_capacity_intelligence.json?t=${Date.now()}`,{cache:'no-store'}).then(r=>r.json()),s=d.summary||{};document.getElementById('cciUpd').textContent=d.updatedAt||'—';document.getElementById('cciCards').innerHTML=[['زيارات بضائع عامة',s.generalCargoCalls||0],['تقديرات متاحة',s.estimatedCalls||0],['تاريخ خاص بالسفينة',s.exactVesselHistorical||0],['عينة Tons/DWT',s.fleetRatioSampleN||0]].map(x=>`<div class="metric"><div class="label">${x[0]}</div><div class="value">${x[1]}</div></div>`).join('');document.getElementById('cciRows').innerHTML=(d.rows||[]).map(r=>`<tr><td>${esc(r.Port)}</td><td><strong>${esc(r.Vessel)}</strong></td><td>${esc(r.IMO)}</td><td>${fmt(r.DWT)}</td><td>${esc(r['Estimation Method'])}</td><td>${fmt(r['Estimated Cargo Tons'])}</td><td>${esc(r.Confidence)}</td></tr>`).join('')||'<tr><td colspan="7">لا توجد بيانات</td></tr>'}catch(e){console.error(e)}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',load,{once:true});else load()})();
